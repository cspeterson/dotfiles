# A Screenrc to start screen with several mutt instances
bindkey "^[[1;2A" focus up
bindkey "^[[1;2B" focus down

# Horizontal split two instances of mutt - 
# one inbox, one nagios
split
screen -t INBOX 1 mutt
screen -t NAGIOS 2 mutt

focus up
select 1
focus down
select 2
focus up
